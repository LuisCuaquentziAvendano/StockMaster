import { Request, Response } from 'express';

class UsersController {
    static registerUser(req: Request, res: Response): void {
        // use createToken
    }

    static verifyUser(req: Request, res: Response): void {
        // use createToken
    }

    static getToken(req: Request, res: Response): void {
        // use createToken
    }

    static setNewToken(req: Request, res: Response): void {
        // use createToken
    }

    static deleteToken(req: Request, res: Response): void {
        // use createToken
    }

    static getInventories(req: Request, res: Response): void {
        // use createToken
    }

    static deleteUser(req: Request, res: Response): void {
        // use createToken
    }

    static editPassword(req: Request, res: Response): void {
        // use createToken
    }

    static createToken(): string {
        return '';
    }
}

export default UsersController;